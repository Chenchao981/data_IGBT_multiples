# 数据IGBT多封装厂 — 项目交接文档

> **项目名称**：data_IGBT_multiple  
> **项目简介**：FT（Final Test）数据清洗工具，支持多个封装厂的测试数据清洗和标准化输出  
> **作者**：cc  
> **交接日期**：2025-06-01  

---

## 目录

1. [项目结构](#1-项目结构)
2. [开发环境](#2-开发环境)
3. [封装厂总览](#3-封装厂总览)
4. [日月新 (Riyuexin)](#4-日月新-riyuexin)
5. [杰群 数据格式1 (Jiequn · 分文件)](#5-杰群-数据格式1)
6. [杰群 数据格式2 (Jiequn · 单文件)](#6-杰群-数据格式2)
7. [PAT 统计](#7-pat-统计)
8. [CSV 解析器 (csv_parser)](#8-csv-解析器)
9. [GUI 界面](#9-gui-界面)
10. [如何添加新封装厂](#10-如何添加新封装厂)
11. [常见问题](#11-常见问题)

---

## 1. 项目结构

```
data_IGBT_multiple/
│
├── factories/                     ← ★ 各封装厂模块（核心代码）
│   ├── base/
│   │   ├── __init__.py
│   │   └── base_cleaner.py        ← 抽象基类（可继承的 process_all + 单位换算）
│   │
│   ├── riyuexin/                  ← 日月新（ASE）
│   │   ├── __init__.py
│   │   ├── config.py
│   │   ├── dc_cleaner.py
│   │   ├── dvds_cleaner.py
│   │   └── rg_cleaner.py
│   │
│   └── jiequn/                    ← 杰群
│       ├── __init__.py
│       ├── config.py              ← 配置 + 单位换算表
│       ├── csv_parser.py          ← ★ CSV 解析器（所有杰群共用）
│       ├── dc_auto.py             ← ★ DC-AI 自动识别与清洗分发
│       ├── formatting.py          ← ★ 输出列名 + 参数排序（格式1/格式2共用）
│       ├── dc_cleaner.py          ← 数据格式1 DC
│       ├── dvds_cleaner.py        ← 数据格式1 DVDS
│       ├── rg_cleaner.py          ← 数据格式1 RG
│       ├── clean_unified.py       ← 数据格式2（单文件 → DC+DVDS+RG）
│       └── pat_cleaner.py         ← PAT 统计
│
├── shared/
│   ├── __init__.py
│   └── excel_utils.py             ← 共享工具（Excel/CSV 读写、文件扫描、文件命名）
│
├── gui/
│   ├── __init__.py
│   ├── main_window.py             ← 侧边栏主窗口（选厂 + 面板切换）
│   ├── start_gui.bat              ← Windows 启动脚本
│   └── panels/
│       ├── __init__.py
│       ├── base_panel.py          ← 面板基类（文件夹选择、按钮、日志）
│       ├── riyuexin_panel.py      ← 日月新面板（3 按钮）
│       └── jiequn_panel.py        ← 杰群面板（DC-AI + 手工入口 + 清洗后统计）
│
├── data/                          ← 原始测试数据（各厂自建目录，不入库）
├── output/                        ← 清洗后数据（各厂自建目录，不入库）
│
├── requirements.txt               ← Python 依赖
└── README.md                      ← 使用文档（建议先读）
```

### 关键文件说明

| 文件 | 重要性 | 说明 |
|------|--------|------|
| `factories/base/base_cleaner.py` | ★★★ | 抽象基类，所有 Cleaner 的父类，内含 `_apply_unit_conversions()` |
| `factories/jiequn/csv_parser.py` | ★★★ | CSV 解析器，所有杰群数据解析依赖此模块 |
| `factories/jiequn/dc_auto.py` | ★★★ | DC-1 / DC-统一CSV / DC-3 自动识别与清洗分发 |
| `factories/jiequn/config.py` | ★★★ | 杰群配置，单位换算表在这里定义 |
| `shared/excel_utils.py` | ★★ | 共享工具，注意 `generate_lot_based_filename` 用于文件名生成 |
| `gui/main_window.py` | ★★ | 主窗口，添加新厂需要在这里注册 |
| `gui/panels/base_panel.py` | ★★ | 面板基类，添加新厂面板可以继承这个 |

---

## 2. 开发环境

### 环境要求

- **OS**：Windows 10/11
- **Python**：3.7+（推荐 3.10+）
- **包管理**：pip
- **Shell**：CMD / PowerShell（项目包含 `.bat` 启动脚本）

### 安装

```bash
pip install -r requirements.txt
```

`requirements.txt` 内容：

```
pandas>=2.2.0,<2.4.0       ← 数据处理核心
openpyxl>=3.0.0             ← .xlsx 读写
PyQt5>=5.15.0               ← GUI
python-calamine>=0.2.0      ← 快速 Excel 读取
xlsxwriter>=3.0.0           ← 快速 Excel 写入
xlrd                        ← .xls 老格式支持（可选）
```

### 数据目录规范

```
data/                        ← 原始数据（各封装厂自建子目录，不入库）
  ├── 杰群/                  ← 杰群厂线A（分文件格式）
  │   └── <产品名> PAT/
  │       ├── DC/
  │       ├── DVDS/
  │       └── RG/
  └── 杰群2/                 ← 杰群厂线B（单文件格式）
      └── RAW/

output/                      ← 清洗后输出（各封装厂自建子目录，不入库）
  ├── 杰群-output/           ← 杰群格式1 输出
  └── 杰群2/                 ← 杰群格式2 输出
```

---

## 3. 封装厂总览

| 封装厂 | 代码位置 | 数据格式 | 支持的数据类型 | 单位换算 | 当前状态 |
|--------|---------|----------|---------------|----------|---------|
| **日月新 (ASE)** | `factories/riyuexin/` | .xlsx | DC / DVDS / RG | 无（数据已是目标单位） | ✅ 稳定，**禁止修改** |
| **杰群 格式1** | `factories/jiequn/` (dc/dvds/rg) | .csv DTA 分文件 | DC / DC散点图 / DVDS / RG | IDSS/IGSS/ISGS→nA, RDON→mR, DVDS→mV | ✅ 稳定 |
| **杰群 格式2** | `factories/jiequn/` (clean_unified) | .csv DTA 单文件 | DC+DVDS+RG 一次出 / DC散点图 | 同上 | ✅ 稳定 |
| **杰群 第三产线** | `factories/jiequn/` (dc_cleaner) | .csv 产品目录平铺、可变尾空列 | DC / DC散点图 | 同格式1 | ✅ 可用 |
| **杰群 PAT** | `factories/jiequn/pat_cleaner.py` | — | 统计汇总 | — | ✅ 可用 |
| **电基 FT-ALL** | `factories/dianji/` | PowerTECH Tab 文本（伪 `.xls`） | 综合 FT 清洗 / FT散点图 | 源单位优先，按头部校验 | ✅ 可用 |
| **电基 PAT** | `factories/dianji/pat_cleaner.py` | 清洗后 `RAW/RAW_n` Excel | 参数统计汇总 | 与日月新相同 | ✅ 可用 |

### GUI 按钮对应关系

杰群面板分三层：
- 第一行：原始数据文件格式/清洗入口；`DC-AI` 为默认入口，另保留 DC-1、DC-统一CSV、DC-3、DVDS、RG 手工入口。
- 第二行：清洗后统计/分析方法（PAT），用于对清洗后的输出数据做统计。
- 输入/输出默认路径与日月新一致，启动后都指向用户桌面；选择按钮不会覆盖用户已选目录。

| 按钮 | 调用的脚本 | 数据格式 |
|------|-----------|---------|
| DC-AI | `factories/jiequn/dc_auto.py` | 自动判断三种 DC 格式后分发 |
| DC-1 | `factories/jiequn/dc_cleaner.py` | 格式1: 分文件 |
| DC-3 | `factories/jiequn/dc_cleaner.py` | 第三产线平铺/产品目录 |
| DVDS | `factories/jiequn/dvds_cleaner.py` | 格式1: 分文件 |
| RG | `factories/jiequn/rg_cleaner.py` | 格式1: 分文件 |
| DC-统一CSV | `factories/jiequn/clean_unified.py` | 格式2: 单文件 |
| PAT | `factories/jiequn/pat_cleaner.py` | 清洗后数据统计 |

### DC-AI 识别边界

- 路径中存在名称严格等于 `DC` 的分类型目录 → `DC-1`。
- 平铺 DTA 的 Item 同时含 DC、`DVDS*`、`LCR-RG` → `DC-统一CSV`。
- 没有 `DC` 子目录，Item 有 DC 参数但不含 `DVDS*` → `DC-3`。
- 混合目录、缺 Item、无 DC 参数、DVDS 缺 LCR-RG、多个统一CSV子目录 → 报错且不清洗。
- 识别阶段只读取 DTA 前40行头部；业务清洗仍由原 cleaner 完成。
- 四个杰群 DC 入口清洗成功后返回本次明确的散点清单；规格与测量值执行同一单位换算，P 型反向 Min/Max 按数值上下界判断并保留原始文本。

### 电基 FT-ALL 边界

- 源文件扩展名是 `.xls`，内容必须以 `PowerTECH Test System` 开头；普通 Excel 良率文件不能混入清洗目录。
- 文件名拆出产品、制造批次、批次标识和测试时间；批次标识兼容旧式 `C203133.03`
  周记及已验证的新式 `FA65-5405`，并校验头部 `Lot:` 一致性；仅允许
  制造主批和批次标识一致时的片号后缀未刷新，并在 GUI 输出 WARNING。
- 以 `DVDS(mV)` 是否有效决定保留行；后续提前失效字段保留为空值。
- 输出先创建 `<产品主体>_NNN` 流水目录；例如完整产品 `NCEAP016N85LL(M)-3E00`
  第一次进入 `NCEAP016N85LL(M)_001`，目录内保存 `<完整产品> DJ PAT.xlsx` 的 `RAW` 工作表。
- FT-ALL 同时生成独立命名的散点数据包；每个 PowerTECH 文件保留独立 Source_ID、上下限和 Bias，GUI 清洗成功后启用“FT散点图”。
- 电基面板第二行提供 `PAT`，可多选一个或多个上述清洗结果；先合并
  `RAW/RAW_n` 中同名参数的全部数据，再按日月新算法生成 PAT 报表。
- 详细 Item 映射和限制见 `docs/电基PowerTECH清洗说明.md`。

---

## 4. 日月新 (Riyuexin)

### ⚠️ 约束

> **日月新代码是稳定版，只迁不改。**  
> 以后如要修改日月新的清洗逻辑，请复制一份出来改，不要动 `factories/riyuexin/` 下的文件。

### 输入

| 项目 | 值 |
|------|-----|
| 格式 | .xlsx |
| 目录结构 | `ASEData/{DC,DVDS,RG}/` |
| 文件来源 | ASE（日月新）测试设备导出 |
| 默认路径 | `ASEData/DC`、`ASEData/DVDS`、`ASEData/RG` |

数据文件格式（以 DC 为例）：

| 行 | 内容 | 说明 |
|----|------|------|
| 第2行 | 参数名行（含 CONT, IDSS, VTH...） | 解析器通过此行动态匹配 |
| 第5行 | 测试条件行（如 40.0 表示 IDSS@40V） | 用于参数增强 |
| 第7行 | 单位行（A, V...） |  |
| 第19行 | "Test No." 表头 | 数据起始标志 |
| 第20行+ | 数据行：NUM, 参数值... | |

### 输出

| 列 | 说明 |
|----|------|
| NUM | 行号（从1开始） |
| lot_ID | 从文件名正则提取的批次号（如 `FA4Z-2484`） |
| 参数列 | 动态列名，如 `IDSS40(A)`, `VTH1(V)`, 单位保留原始 |

输出文件名：`{lot_ID}_{类型}_{时间戳}.xlsx`

### 清洗逻辑

```
scan_excel_files(dir)         ← 扫描 .xlsx 文件
    → extract_dc_data(file)  ← 读取、匹配参数列、参数增强（IDSS40等）
    → merge_all_dc_data()    ← 合并
    → clean_and_format_dc()  ← 去空行、顺序 NUM
    → save_dc_result()       ← 保存为 Excel
```

### 关键函数

| 函数 | 位置 | 作用 |
|------|------|------|
| `DCDataCleaner.process_all_dc_files()` | `dc_cleaner.py:169` | 主入口：扫描→提取→合并→保存 |
| `extract_dc_data()` | `dc_cleaner.py:103` | 单文件提取，包含参数增强 |
| `extract_test_condition_value()` | `dc_cleaner.py:64` | 从第5行提取测试条件数值 |
| `generate_lot_based_filename()` | `shared/excel_utils.py` | 生成输出文件名 |

---

## 5. 杰群 数据格式1

### 适用场景

杰群厂线A，DC/DVDS/RG 数据分别放在不同子目录中。

### 输入

| 项目 | 值 |
|------|-----|
| 格式 | .csv（DTA 格式） |
| 目录结构 | `<根>/DC/`, `<根>/DVDS/`, `<根>/RG/` |
| 文件命名 | DC: `*DTA.CSV` / DVDS: `*_DVDSDTA.CSV` / RG: `*_RGDTA.CSV` |
| 默认路径 | `data/杰群/` |

CSV 头部结构（通用 DTA 格式）：

```
行  内容                    ← 说明
──  ────────────────────    ← ─────────────
~1  DTA File Name,...       ← 元数据
~15 Test,,1,2,3,...         ← 测试编号
~16 Item,,IDSS,VTH,...      ← ★ 参数名称（解析器靠此行动态匹配）
~17 Min Limit,...
~18 Max Limit,...
~19 Limit Units,,A,V,...    ← 单位行
~20 Bias 1,,,IDS,...        ← Bias 描述
~21 Bias 1 Value,,1E-2,...  ← ★ 测试条件值（用于参数增强命名）
~31 Min Result,...
~32 Max Result,...
~33 Average,...
~34 STD DEV,...
~35 Serial,Bin,...          ← 数据表头
~36 1,1,val,val,...         ← 数据行从此开始
```

### 输出

| 列 | 说明 |
|----|------|
| NUM | 行号（从1开始） |
| 批次 | 文件名中 `split('_')[1]` 提取的批次标识（内部解析列可能仍叫 `周记`，最终输出统一改名） |
| 参数列 | 增强命名 + 单位 |

输出文件名：`mixed_{类型}_JQ_{时间戳}.xlsx`

### 参数增强命名规则

| 规则 | 示例 | 对应参数 | 来源 |
|------|------|---------|------|
| seq | VTH1(V), BVDSS1(V) | VTH, BVDSS | 顺序编号 |
| bias | IDSS100(nA), RDON40(mR), IGSS25(nA) | IDSS, ISGS, IGSS, RDON | Bias 1 Value 行；同名有效 IDSS/RDON 测试全部保留并按源顺序在单位前加 `-1/-2`；杰群负偏置 ISGS 保留为 ISGS，正偏置 ISGS 改名为 IGSS |
| unit | DVDS(mV), VFSD(V) | DVDS, VF, VFSDS, CONT | 仅加单位 |

### 输出参数排序（2026-06-03 更新）

杰群格式1和格式2共用 `factories/jiequn/formatting.py`：

```text
NUM, 批次, VTH*, BVDSS*, IDSS*, ISGS*/IGSS*, RDON*, LRDON*, VF*, VFSD*, VFSDS*, DVDS*, RG*, CONT*, ABSDEL*, DELAY*
```

注意：
- 杰群 `ISGS-25/-20/-10` 这类负偏置列会输出为 `ISGS25/20/10`；正偏置 `ISGS+25/+20/+10` 输出为 `IGSS25/20/10`，同一偏置值保持 `ISGS` 在前、`IGSS` 在后。
- `VF` 使用精确匹配，不再误匹配 `VFSDS`。
- `RDON` 和历史写法 `Rdson` 兼容，单位统一换算为 `mR`。

### 单位换算

| 参数 | 原始→目标 | 因子 | 定义位置 |
|------|-----------|------|---------|
| IDSS | A → nA | ×1e9 | `config.py:UNIT_CONVERSIONS` |
| IGSS | A → nA | ×1e9 | 同上 |
| ISGS | A → nA | ×1e9 | 同上 |
| RDON | Ω → mR | ×1000 | 同上 |
| DVDS | V → mV | ×1000 | 同上 |

换算在 `BaseCleaner._apply_unit_conversions()` 自动执行。

### 清洗流程

```python
# DC 示例
JiequnDCCleaner(input_dir, output_dir).process_all()
# 内部:
#   1. _get_dc_subdir()    ← 自动查找 DC/ 子目录
#   2. parse_dta_csv()     ← csv_parser 解析 CSV（含参数增强）
#   3. concat(所有文件)     ← 合并
#   4. _apply_unit_conversions()  ← 按 config 换算
#   5. 插入 NUM 列         ← 行号
#   6. normalize_output_columns() ← 批次列命名 + 参数排序
#   7. write_excel_fast()  ← 保存
```

---

## 6. 杰群 数据格式2

### 适用场景

杰群厂线B，所有参数（DC+DVDS+RG）在同一个 CSV 文件中（45 项测试/行）。

### 输入

| 项目 | 值 |
|------|-----|
| 格式 | .csv（DTA 格式） |
| 目录结构 | `<根>/`（单层目录，全部 CSV 文件混放） |
| 文件命名 | `*DTA.CSV` |
| 默认路径 | `data/杰群2/RAW/` |

CSV 结构与格式1一致，但 Item 行包含所有参数类型（VTH+BVDSS+IDSS+ISGS+RDON+VF+VFSDS+DVDS_EX+LCR-RG）。

### 格式2 输出规则（2026-06-03 更新）

- 按源 CSV `Item` 行从左到右的顺序输出参数，方便和源数据逐列对比。
- 剔除 `CONT*` 计数字段（如 `CONT_TR`、`CONT_RF`、`CONT-B`、`CONT-C`、`CONT-E`）和 `SAME` 占位字段。
- DC 参数白名单为 `LCR-RG`、跳过第一个 `VTH` 后的所有 `VTH`、`BVDSS`、跳过第一个 `IDSS` 后的所有 `IDSS`、`ISGS`、`RDON`、`VFSDS`、`ABSDEL`。
- DC 会跳过源数据从左到右发现的第一个 `VTH` 和第一个 `IDSS`（封装厂参数）；后续 `VTH` 从 `VTH1(V)` 开始重新编号；后续 `IDSS` 按 `Bias 1 Value` 命名，如 `IDSS4(nA)`、`IDSS3.5(nA)`，同名时输出为 `IDSS3.5-1(nA)`、`IDSS3.5-2(nA)`。
- DC 输出单位沿用杰群规则：`IDSS/IGSS/ISGS` 输出为 `nA`，`RDON` 输出为 `mR`，并执行对应数值换算；DVDS 输出为 `DVDS(mV)`。
- `DVDS(mV)` 为空的记录行会在输出前删除，空值不参与后续统计。
- 内部解析列仍叫 `周记`，最终输出列名统一为 `批次`。

### 输出

一次运行输出三个文件：

| 文件 | 列 | 行数参考 |
|------|----|---------|
| `mixed_DC_JQ2_*.xlsx` | NUM + 批次 + DC 参数 | ~500K 行 |
| `mixed_DVDS_JQ2_*.xlsx` | NUM + 批次 + DVDS(mV) | ~500K 行 |
| `mixed_RG_JQ2_*.xlsx` | NUM + 批次 + RG(R) | ~500K 行 |

### 调用方式

```python
# CLI
python factories/jiequn/clean_unified.py data/杰群2/RAW output/杰群2

# Python API
from factories.jiequn.clean_unified import run
run("data/杰群2/RAW", "output/杰群2")
```

### 与格式1的区别

| 项目 | 格式1 | 格式2 |
|------|-------|-------|
| 文件数量 | 每个类型多个 CSV 文件 | 一个 CSV 文件含全部 |
| 解析方式 | 按类型分别扫描子目录 | 同文件按参数名匹配，并保持源 Item 行顺序 |
| DC 参数 | 34 个（含 CONT, VFSDS2等） | 按 `DC_PARAMS` 白名单，含 `LCR-RG`，跳过第一个 `VTH` |
| DVDS | `DVDS(mV)`（单列） | `DVDS(mV)`（单列） |
| RG | `RG(R)`（单列） | `RG(R)`（单列） |

---

## 7. PAT 统计

### 输入

从已清洗的 DC/DVDS/RG 输出文件读取；电基从清洗结果的 `RAW/RAW_n` 工作表读取。
GUI 支持一次显式选择一个或多个工作簿，同名参数先合并全部原始值再计算。

### 公式

```
Sigma = (Q3 - Q1) / 1.35
LCL = 中位数 - 6 * Sigma
UCL = 中位数 + 6 * Sigma
```

### 输出

每个参数一行：

| 列 | 说明 |
|----|------|
| 统计量 | 参数名 |
| 总计数 | 有效数据条数 |
| 均值 / 标准差 | 基本统计 |
| 最小值 / 下四分位数 / 中位数 / 上四分位数 / 最大值 | 分位数 |
| Sigma | (Q3-Q1)/1.35 |
| LCL计算值 / UCL计算值 | 中位数 ± 6×Sigma |
| LCL更新前 / UCL更新前 | 上一次确定的值（手动填写，程序填 NaN） |
| LCL更新后 / UCL更新后 | 本次更新值（程序填 NaN） |
| 是否更新 | 程序填 NaN |

---

## 8. CSV 解析器 (csv_parser)

这是杰群数据解析的**核心模块**，所有杰群 cleaner 都依赖它。

### `parse_dta_csv(file_path, target_params, unique_only=False)`

从单个 DTA CSV 中提取目标参数，返回含增强列名的 DataFrame。

| 参数 | 类型 | 说明 |
|------|------|------|
| `file_path` | str | CSV 文件路径 |
| `target_params` | list[str] | 要提取的参数基名，如 `["VTH", "IDSS"]` |
| `unique_only` | bool | True=每个参数只取第一列，False=取所有实例 |

**内部流程**：

1. `read_header_info()` — 扫描头部，找到 Item / Limit Units / Bias 1 Value / Serial 行
2. 匹配 `target_params` 在 Item 行中的列位置（使用 `_item_matches_param()`，避免 `VF` 误匹配 `VFSDS`）
3. `_build_param_name()` — 按规则（seq/bias/unit）构造增强列名
4. `pd.read_csv()` — 读取数据（跳过头部，指定列数）
5. 返回 DataFrame（列含 周记）

### 辅助函数

| 函数 | 用途 |
|------|------|
| `extract_zhouji(filename)` | 从文件名提取周记（`split('_')[1]`） |
| `read_header_info(file_path)` | 读取 CSV 头部元信息 |
| `locate_key_rows(file_path)` | 定位 Item / Serial 关键行 |
| `_build_param_name(base, rule, bias, unit, counter)` | 构建增强参数名 |
| `_get_bias_value(bias_values, csv_field_idx)` | 获取某列的 Bias 1 Value |
| `_item_matches_param(item_name, target_param)` | 精确/别名匹配 Item 名称，处理 `RDON/Rdson`、`LCR-RG`、`VF/VFSDS` 等 |

### 2026-06-03 解析规则更新

- 目标参数不再使用简单子串包含匹配，短参数名不会误吃长参数名。
- 杰群负偏置 `ISGS` 保留为 `ISGS` 并去掉负号，例如 `ISGS-25(nA)` → `ISGS25(nA)`；正偏置 `ISGS+25(nA)` → `IGSS25(nA)`。
- 输出格式由 `factories/jiequn/formatting.py` 统一处理，最终对外列名是 `批次` 而不是 `周记`。

### 关键：列数推断

CSV 中不同 Bin 的数据行字段数可能不一致。解析器会先读 Serial 行统计列数，然后 `pd.read_csv(names=range(n_cols))` 强制指定列数，避免首行字段少导致列数误判。

---

## 9. GUI 界面

### 架构

```
main_window.py
├── QListWidget (左侧: 封装厂列表)
└── QStackedWidget (右侧: 面板)
    ├── RiyuexinPanel (3 按钮: DC/DVDS/RG)
    ├── JiequnPanel (第一行: DC-AI/DC-1/DC-统一CSV/DC-3/DVDS/RG; 第二行: PAT)
    └── DianjiPanel (FT-ALL 清洗 + SYL&SBL)
```

### 添加新厂

1. 在 `gui/panels/` 下建面板文件，继承 `BasePanel`
2. 在 `gui/main_window.py` 的 `FACTORIES` 列表加入

```python
FACTORIES = [
    {"name": "日月新 (ASE)", "panel": RiyuexinPanel},
    {"name": "杰群 (Jiequn)", "panel": JiequnPanel},
    {"name": "新厂", "panel": NewFactoryPanel},  # ← 加在这里
]
```

### 面板基类 BasePanel

| 方法 / 属性 | 说明 |
|-------------|------|
| `data_types` | 原始数据文件格式/清洗入口按钮列表，如 `["DC-AI", "DC-1", "DC-统一CSV", "DC-3", "DVDS", "RG"]` |
| `post_process_types` | 清洗后处理/统计分析按钮列表，如 `["PAT"]` |
| `factory_name` | 厂名 |
| `_get_cleaner_fn(type)` | 返回一个无参 callable，执行清洗 |
| `set_default_paths(in, out)` | 设置默认输入输出路径 |

---

## 10. 如何添加新封装厂

### 最小步骤

```
1. mkdir factories/新厂/
2. 创建 config.py（厂名、数据类型、单位换算）
3. 创建 cleaner.py，继承 BaseCleaner，实现 process_all()
4. 创建 gui/panels/新厂_panel.py，继承 BasePanel
5. 在 gui/main_window.py 的 FACTORIES 列表注册
```

### 如果数据是 CSV

复用 `factories/jiequn/csv_parser.py`：

```python
from factories.jiequn.csv_parser import parse_dta_csv

df = parse_dta_csv("input.csv", ["IDSS", "VTH"], unique_only=False)
```

### 如果数据是 Excel

复用 `shared/excel_utils.py`：

```python
from shared.excel_utils import read_excel_fast, write_excel_fast
df = read_excel_fast("input.xlsx")
write_excel_fast(df, "output.xlsx")
```

---

## 11. 常见问题

### Q: 清洗报错 "not found DC directory"

输入目录结构不匹配。检查：
- 格式1：输入目录下应该有 `DC/` / `DVDS/` / `RG/` 子目录
- 格式2：输入目录下应该直接放 `*DTA.CSV` 文件

### Q: 参数名不匹配（如期望 IDSS40 实际输出 IDSS_1）

`csv_parser.py` 的 `_PARAM_NAME_RULES` 中的基名和 Item 行中的参数名需要匹配。检查 Item 行中的实际参数名与目标参数名是否一致。

### Q: GUI 启动报错

```bash
# 确保在项目根目录运行
python gui/main_window.py
```

### Q: 数据量大，清洗慢

| 数据量 | 预估时间 |
|--------|---------|
| DC 格式1（9文件/900K行） | ~5 分钟 |
| DC 格式2（20文件/500K行） | ~3 分钟 |
| DVDS/RG | ~1 分钟 |

### Q: 功能性调整完成后要做什么

1. 跑对应清洗命令和 `python -m py_compile ...` 做基础验证。
2. 只提交代码和说明文档，不提交 `output/`、`__pycache__/`、失败的临时 Excel。
3. 完成功能性调整后提交并推送到 GitHub 仓库，保持远端同步，方便下次迭代接手。

---

## 附录：命令速查

```bash
# 安装
pip install -r requirements.txt

# 训练用（非模型，指清洗）：
# 日月新
python factories/riyuexin/dc_cleaner.py
python factories/riyuexin/dvds_cleaner.py
python factories/riyuexin/rg_cleaner.py

# 杰群 格式1（分文件）
python factories/jiequn/dc_cleaner.py
python factories/jiequn/dvds_cleaner.py
python factories/jiequn/rg_cleaner.py

# 杰群 格式2（单文件）
python factories/jiequn/clean_unified.py data/杰群2/RAW output/杰群2

# 杰群 DC-AI（推荐）
python factories/jiequn/dc_auto.py <输入目录> <输出目录>

# PAT
python -c "from factories.jiequn.pat_cleaner import build_pat, save_pat; save_pat(build_pat('output/杰群-output'))"

# 电基 PAT
python -c "from factories.dianji.pat_cleaner import generate_pat; generate_pat(source_files=[r'<清洗结果.xlsx>'], output_dir=r'<输出目录>')"

# GUI
python gui/main_window.py

# 测试 import 是否正常
python -c "from factories.base.base_cleaner import BaseCleaner; from shared.excel_utils import read_excel_fast; print('OK')"
```

---

**最后更新**：2026-07-22
**本次更新重点**：杰群四个 DC 入口和电基 FT-ALL 增加 FT 参数散点图；规格按源文件追溯、同步单位换算，GUI 与发布包均已接入并用真实数据验证。
