# FT 直接应用 Y 轴和参数选择优化

日期：2026-09-08；版本：v2.22.2。

## 交互

- 移除“自定义 Y 轴范围”复选框，保留文字标题；填写上下限后点击“应用自定义并绘制”即启用指定范围。
- 增加“恢复自动范围”按钮，恢复自动绘图，同时保留输入内容以便再次应用。
- 参数首次打开/更换数据版本时默认空选；右侧“全选”按钮一次选中全部参数，仍支持自由多选、删选及清空。
- 选择变化后仍需点击“绘制图形”，不会自动对所有参数启动绘图。
- 图型和各参数范围相互独立；无效或空白边界明确报错、不显示旧图。未改动清洗、FT 分组、PAT、良率和箱体统计。

## 验证与交付

- 全量回归 155 项通过（16.119 秒）；图表 19 项涵盖默认空选、全选、全选后单选/多选/清空、数据版本切换重置、无复选框直接应用、非法边界、恢复自动和图型/参数隔离。
- 隔离发布包及实际 Anaconda 环境：四个 GUI 面板正常；真实杰群数据包 30 参数全选正常，VTH2(V) 直接应用 -0.5～5、恢复自动、PNG 显示均正常。
- 浏览器复核常用入口：默认空选、全选按钮，无多余默认值冲突提示。
- 根 `packaging/release/` 的 PYZ 与物理前端同步到 v2.22.2；端口 8502、8503、8505、8506、8507 的根入口 FT 服务已重启，原端口保持不变。未操作 CP、TMS 或历史版本服务。
- 旧根运行文件备份：`C:/Users/wgs03/AppData/Local/Temp/ft-release-before-222-5202a293602d4e80b747e6a578202a7a/`。原数据未修改；历史 v2.22.1 完整发布副本仍保留。
- 当前启动入口：`packaging/release/startup_program.bat`。已打开页面刷新后重新选参数绘图，无需重新清洗。
- PYZ SHA256：`49d668028df456b54a9b9321c5db262d9d3818971984409a743700f8e67f9179`。

版本化子目录保留独立交付副本；根发布文件按仓库既有跟踪规则更新。
