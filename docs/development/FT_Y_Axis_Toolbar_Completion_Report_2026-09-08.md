# FT Y 轴单行操作栏完成报告

日期：2026-09-08；版本：v2.22.1。

## 本次调整

散点图和箱体图上方的 Y 轴设置改为直接显示的单行操作栏：自定义开关、Y 轴最小值标签及输入框、Y 轴最大值标签及输入框、右侧“应用自定义并绘制”。移除原来的折叠框、图前绘制规则说明和表单内操作说明。多参数时每张图保留参数标识和独立操作栏。

仍然只在点击应用后修改范围；取消勾选并应用恢复自动范围。保留负数、小数、科学计数法校验、图型与参数设置隔离、PNG 下载和缓存逻辑。未修改渲染器、FT 分组、数据过滤或统计算法。

## 验证

- 图表交互/统计回归 19 项通过；新增检查六列横向结构、按钮文字、无折叠框及已移除说明。
- 全量回归 155 项通过（8.857 秒），含清洗和报表既有测试。
- 发布包真实电基数据浏览器验证：103,282 行、7 批次，VTH1(V) 有效值 102,650；散点图应用 -0.5～6 后重新生成，页面显示精确边界，下载按钮正常。
- 1280 像素宽浏览器截图检查：开关、两个标签与输入框、右侧按钮均在同一行，图形紧接其下，无折叠框或旧说明。
- 隔离导入发布 PYZ，Qt GUI 四个工厂面板构造通过；75 个归档条目，无原始 Excel/CSV、日志、缓存或内部 Markdown。
- 源码、PYZ 和物理前端入口字节一致；git diff --check 通过。

## 交付

运行 `packaging/release/FT_Data_Cleaner_v2.22.1/startup_program.bat`。分发时保留整个目录，包括 `frontend`、依赖清单和启动脚本。之前的发布目录与运行中的用户程序未覆盖或停止。

PYZ SHA256：`2938493aca6dcad88b0c470c1dbe568e5b8c19c6bbfd6f9426ce78f23199b08d`。

版本化发布目录按现有仓库规则仅本地交付，不强制加入 Git；源码、测试和说明进入版本管理。

## 常用入口同步修复（同日追加）

用户反馈截图仍无输入框。检查发现端口 8502、8503、8505、8506 的 FT 图表进程仍加载根 `packaging/release/frontend/ft_scatter_app.py`，该入口与根 PYZ 停留在旧版。仅提供独立新版目录没有完成常用入口升级。

已将验证过的 v2.22.1 PYZ、物理前端、使用说明同步至根 `packaging/release/`，只重启上述四个 FT 图表服务并保留原端口；CP、TMS 及独立 v2.22.0 服务未停止。旧根发布文件备份于 `C:/Users/wgs03/AppData/Local/Temp/ft-release-before-y-axis-5ba06ff5f98c4445a097b31eae403495/`，可恢复。

使用实际 Anaconda 环境验证根发布包：四个 GUI 面板构造正常，源码与物理前端匹配，Y 轴数值校验正常，19 项图表测试通过。根发布 PYZ 的 SHA256 与上述 v2.22.1 相同。根发布文件按仓库已有跟踪规则更新。

用户继续使用 `packaging/release/startup_program.bat` 即可；已打开的旧页面刷新后，重新选择参数并绘制，再输入自定义范围。不必重新清洗数据。
